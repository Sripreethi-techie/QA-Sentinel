# Moved

This folder previously held the monolith. The project now lives at the repository root:

- `backend/` — Spring Boot
- `frontend/` — Vite + React
- `qa-runner/` — Playwright

See the root [README.md](../README.md) and [RUNBOOK.md](../RUNBOOK.md).

You can delete this `AgenticPMO` directory if you no longer need a pointer.
